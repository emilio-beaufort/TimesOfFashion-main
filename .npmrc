# Force npm to handle optional dependencies properly
optional=false
# Use legacy peer deps to avoid conflicts
legacy-peer-deps=true
# Disable audit during install
audit=false
# Use exact versions
save-exact=true
